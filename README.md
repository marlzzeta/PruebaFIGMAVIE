
  # Document integration (copia)

  This is a code bundle for Document integration (copia). The original project is available at https://www.figma.com/design/tATviV6JrW8Pkexi3b91C0/Document-integration--copia-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  